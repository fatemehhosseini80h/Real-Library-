# مستندات طراحی پروژه Real-Library

## 🎯 هدف پروژه
طراحی یک سیستم ساده برای مدیریت کتابخانه با قابلیت ثبت کتاب، عضو، و امانت‌گیری.

## 🧩 اجزای سیستم
- `Book`: کلاس مربوط به کتاب‌ها
- `Member`: کلاس مربوط به اعضای کتابخانه
- `Loan`: کلاس مربوط به فرآیند امانت گرفتن کتاب

## 📊 نمودار Use Case (متنی)
- ثبت کتاب جدید (مدیر)
- ثبت عضو جدید (مدیر)
- مشاهده لیست کتاب‌ها (عضو)
- مشاهده لیست اعضا (عضو)
- امانت گرفتن کتاب (عضو)

## 🔄 جریان فعالیت (Activity)
۱. عضو وارد سیستم می‌شود  
۲. لیست کتاب‌ها را مشاهده می‌کند  
۳. کتاب مورد نظر را انتخاب می‌کند  
۴. اگر کتاب موجود باشد، فرآیند امانت انجام می‌شود

## 🗃️ پایگاه داده / ذخیره‌سازی
در این نسخه، اطلاعات در حافظه ذخیره می‌شوند (بدون دیتابیس)

## 👥 کاربران سیستم
- مدیر کتابخانه
- اعضای کتابخانه

## 📌 نکات طراحی
- استفاده از برنامه‌نویسی شی‌گرا (OOP)
- جداسازی کلاس‌ها در فایل‌های مستقل
- استفاده از شرط برای بررسی موجود بودن کتاب
