# 📚 Real-Library Project

پروژه‌ای ساده برای مدیریت کتابخانه با استفاده از برنامه‌نویسی شی‌گرا در Python.

---

## 🧩 ساختار فایل‌ها

| فایل | توضیح |
|------|--------|
| `main.py` | اجرای اصلی برنامه |
| `book.py` | کلاس مربوط به کتاب‌ها |
| `member.py` | کلاس مربوط به اعضای کتابخانه |
| `loan.py` | کلاس مربوط به فرآیند امانت گرفتن |
| `tests.py` | تست‌های واحد برای بررسی عملکرد کلاس‌ها |
| `documentation.md` | مستندات طراحی و تحلیل سیستم |
| `README.md` | توضیحات کلی پروژه |

---

## 🚀 نحوه اجرا

برای اجرای پروژه، ابتدا مخزن را کلون کرده و سپس فایل اصلی را اجرا کنید:

```bash
git clone https://github.com/fatemehhosseini80h/Real-Library-
cd Real-Library-
python main.pyfrom book import Book
from member import Member
from loan import Loan

if __name__ == "__main__":
    book1 = Book("1984", "George Orwell")
    member1 = Member("Fatemeh")
    loan1 = Loan(book1, member1)
    print(loan1)
