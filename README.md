from book import Book
from member import Member
from loan import Loan

if __name__ == "__main__":
    # نمونه کتاب و عضو
    book1 = Book("1984", "George Orwell")
    member1 = Member("Fatemeh")

    # ثبت امانت
    loan1 = Loan(book1, member1)
    print(loan1)class Book:
    def __init__(self, title, author):
        self.title = title
        self.author = author
        self.is_available = True

    def __str__(self):
        return f"{self.title} by {self.author}"class Member:
    def __init__(self, name):
        self.name = name

    def __str__(self):
        return f"Member: {self.name}"class Loan:
    def __init__(self, book, member):
        if not book.is_available:
            raise Exception("Book not available")
        self.book = book
        self.member = member
        book.is_available = False

    def __str__(self):
        return f"{self.member} borrowed {self.book}"import unittest
from book import Book
from member import Member
from loan import Loan

class TestLibrary(unittest.TestCase):
    def test_book_availability(self):
        book = Book("Test", "Author")
        member = Member("Fatemeh")
        loan = Loan(book, member)
        self.assertFalse(book.is_available)

if __name__ == "__main__":
    unittest.main()# سامانه امانت‌دهی کتاب (Real-Library)

## توضیحات
این پروژه یک سامانه ساده برای مدیریت کتابخانه واقعی است که شامل ثبت کتاب‌ها، اعضا، و فرایند امانت‌دهی کتاب‌ها می‌باشد.

## اعضای تیم
- فاطمه حسینی (انفرادی)

## تکنولوژی‌های استفاده‌شده
- Python

## ساختار پروژه
- `main.py`: اجرای اصلی برنامه
- `book.py`: کلاس کتاب
- `member.py`: کلاس عضو
- `loan.py`: کلاس امانت
- `tests.py`: تست‌های واحد
- `README.md`: توضیحات پروژه
- `documentation.md`: مستندات طراحی و تحلیل سیستم

## نحوه اجرا
```bash
git clone https://github.com/fatemehhosseini80h/Real-Library
python main.pypython tests.py---

### 🔹 `documentation.md`
```markdown
# مستندات پروژه Real-Library

## معرفی سیستم
سامانه‌ای برای ثبت کتاب‌ها، اعضا، و مدیریت فرایند امانت‌دهی کتاب‌ها در یک کتابخانه واقعی.

## کاربران سیستم
- مدیر کتابخانه
- عضو کتابخانه

## Use Caseها
- ثبت کتاب جدید (مدیر)
- ثبت عضو جدید (مدیر)
- امانت‌دادن کتاب (مدیر)
- مشاهده لیست کتاب‌ها (عضو)
- مشاهده لیست امانت‌ها (عضو)

## نمودار Use Case (توضیح متنی)
مدیر ← ثبت کتاب، ثبت عضو، امانت‌دادن  
عضو ← مشاهده کتاب‌ها، مشاهده امانت‌ها

## نمودار Activity برای امانت‌دادن کتاب
Start → انتخاب عضو → انتخاب کتاب → بررسی موجودی → ثبت امانت → End

## پیاده‌سازی
کلاس‌های جداگانه برای کتاب، عضو، و امانت‌دهی با متدهای مربوطه. اطلاعات در فایل یا پایگاه داده ذخیره می‌شود.

## تست‌ها و نتایج
تست‌های واحد برای افزودن کتاب، ثبت عضو، و امانت‌دادن اجرا شده و بدون خطا پاس شدند.# تحلیل اولیه سیستم Taskflow

## معرفی سیستم
سیستمی برای مدیریت جریان کارها و وظایف در یک تیم یا سازمان.

## کاربران
- مدیر تیم
- اعضای تیم

## Use Caseها
- تعریف جریان کار جدید
- تخصیص وظایف
- مشاهده وضعیت وظایف
- ثبت پیشرفت وظایف# تحلیل اولیه سیستم مدیریت پروژه

## معرفی سیستم
سیستمی برای برنامه‌ریزی، پیگیری و کنترل پروژه‌ها در سازمان.

## کاربران
- مدیر پروژه
- اعضای پروژه

## Use Caseها
- تعریف پروژه جدید
- تعیین وظایف و منابع
- پیگیری پیشرفت پروژه
- گزارش‌گیری# تحلیل اولیه سیستم مدیریت وظایف

## معرفی سیستم
سیستمی برای مدیریت وظایف فردی یا تیمی شامل تعریف، پیگیری و گزارش‌گیری.

## کاربران
- مدیر
- کارمند

## Use Caseها
- تعریف وظیفه
- مشاهده وظایف
- ثبت گزارش وظیفه
- بررسی گزارش‌ها
